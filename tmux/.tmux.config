# Doing this cause I want tmux stuff in ~/.config
set-environment -g TMUX_PLUGIN_MANAGER_PATH '~/.config/tmux/plugins'

# ── Terminal Colors ───────────────────────────────────────
set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",xterm-256color:RGB"

# ── Quality of Life ───────────────────────────────────────
set -g mouse on                    # Click panes, scroll, resize
set -g base-index 1                # Windows start at 1, not 0
setw -g pane-base-index 1          # Panes start at 1 too
set -g renumber-windows on         # Re-number when a window closes
set -g history-limit 50000         # Big scrollback buffer
set -sg escape-time 0              # No delay for escape key (vital for vim)
set -g display-time 1500           # Status messages stay visible longer
set -g focus-events on             # Lets vim/neovim detect focus changes

# ── Intuitive Splits ──────────────────────────────────────
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"
unbind '"'
unbind %

# ── Reload Config ─────────────────────────────────────────
bind r source-file ~/.tmux.conf \; display "Config reloaded!"

# ── Catppuccin Theme ──────────────────────────────────────
run ~/.config/tmux/plugins/catppuccin/tmux/catppuccin.tmux
set -g @catppuccin_flavor "mocha"
set -g @catppuccin_window_status_style "rounded"

# ── Plugins ───────────────────────────────────────────────
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'sainnhe/tmux-fzf'

set -g @continuum-restore 'on'

# ── Status Bar ────────────────────────────────────────────
set -g status-position top
set -g status-interval 5
set -g @catppuccin_window_default_text "#W"
set -g @catppuccin_window_current_text "#W"
set -g allow-rename off
set -g status-right-length 100
set -g status-left-length 100
set -g status-right "#{E:@catppuccin_status_application}"
set -ag status-right "#{E:@catppuccin_status_session}"
set -ag status-right "#{E:@catppuccin_status_uptime}"
set -g status-left '#{?client_prefix,#[bg=red bold] PREFIX #[default],}#{E:@catppuccin_status_session}'

# ── FZF config ───────────────────────────────────────────
# Launch key
unbind f
TMUX_FZF_LAUNCH_KEY="f"

# Menu order
TMUX_FZF_ORDER="session|window|pane|command|keybinding"

# Popup style with preview
TMUX_FZF_OPTIONS="-p -w 60% -h 50% -m --preview-window=right:40%"

# Enable pane preview
TMUX_FZF_PREVIEW=1

# Show current pane info in preview
TMUX_FZF_PREVIEW_FOLLOW=1

# Keep this line LAST
run '~/.config/tmux/plugins/tpm/tpm'
